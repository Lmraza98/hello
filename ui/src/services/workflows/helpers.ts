/**
 * Re-export from the canonical location.
 * Kept for backward compatibility — new code should import from
 * `../messageHelpers` (or `@/services/messageHelpers`).
 */
export { msgId, textMsg, statusMsg, buttonsMsg } from '../messageHelpers';
